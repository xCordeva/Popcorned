export const metadata = {
  title: "Popcorned - Search",
  description:
    "Explore and find the latest movies, TV shows, and celebrities on Popcorned. Search through a vast library of entertainment, access information on your favorite titles, and keep track of what's trending.",
};

export default function RootLayout({ children }) {
  return <div>{children}</div>;
}
